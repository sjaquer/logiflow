# Variables de entorno de Firebase
# Copia este archivo a .env.local y reemplaza los valores con tus propias credenciales de Firebase.

# Lo encuentras en Configuración del proyecto > General > Tus apps > App web de Firebase > SDK de Firebase
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Para el script de SEED (poblar base de datos)
# Debes crear una cuenta de servicio en Firebase y descargar el archivo JSON de la clave privada.
# Configuración del proyecto > Cuentas de servicio > Generar nueva clave privada
# Guarda el archivo JSON en la raíz del proyecto (o en una ubicación segura) y pon la ruta aquí.
# Por ejemplo: GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=
